server:
  port: 8201
spring:
  application:
    name: order-service
  cloud:
    config: #Config客户端配置
      profile: application #启用配置后缀名称
      label: master #分支名称
      uri: http://localhost:8202 #配置中心地址
      name: config #配置文件名称